- name: Expand partition
  command: growpart /dev/nvme0n1 1
  failed_when: false

- name: Resize filesystem
  command: resize2fs /dev/nvme0n1p1

# ✅ FINAL — Use passed CR sys_id directly (BEST METHOD 🔥)
- name: Update ServiceNow Change Request
  uri:
    url: "https://nowlearning-nlinst04253758-14pkn-0001.lab.service-now.com/api/now/table/change_request/{{ cr_sys_id }}"
    method: PATCH
    user: admin
    password: 9MpTfxSWeVEe
    headers:
      Content-Type: "application/json"
    body:
      work_notes: "Disk expansion completed via AAP automation ✅"
    body_format: json

