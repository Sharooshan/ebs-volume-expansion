- name: Expand partition
  command: growpart /dev/nvme0n1 1
  ignore_errors: yes

- name: Resize filesystem
  command: resize2fs /dev/nvme0n1p1

- name: Update ServiceNow Incident to Resolved
  uri:
    url: "https://nowlearning-nlinst04253758-14pkn-0001.lab.service-now.com/api/now/table/incident/{{ incident_sys_id }}"
    method: PATCH
    user: admin
    password: 9MpTfxSWeVEe
    headers:
      Content-Type: "application/json"
    body:
      state: "6"
      close_notes: "Resolved automatically via AAP automation"
    body_format: json

